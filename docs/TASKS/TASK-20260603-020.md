# TASK-20260603-020
estado: SPEC_SEQUENTIAL_ONLY
owner: opencode
aprobador: Codex Orquestador
objetivo: Integrar al pipeline Chatwoot los modulos core ya aprobados (`routines`, `workouts`, `timers`, `progress`, `plans`, `protocols`) en una pasada controlada, despues de mergear y revisar sus tasks core.
alcance_permitido: actualizar `coarseClassifier`, `moduleIntentClassifier`, `moduleRouter`, `actionExecutor`, `responseComposer`, `bufferProcessor`, `server.ts`; registrar handlers reales; actualizar session-context; agregar tests de pipeline y regresion; documentar estado productivo pendiente; preparar deploy controlado
alcance_prohibido: ejecutar en paralelo con tasks core; deploy sin revision Codex; cambiar secretos; cambiar scope Chatwoot; tocar tablas sin prefijo `sara_`; crear entidades no previstas; modificar reglas de negocio internas de los modulos core salvo bug bloqueante aprobado; confirmar acciones sin evidencia real; generar acciones automaticas no solicitadas
validaciones: `npm run typecheck`, `npm test`, `npm run build`
evidencia: salida de validaciones, diff revisable, docs actualizados, commit con formato obligatorio

## Precondiciones

Esta task NO puede comenzar hasta que Codex Orquestador haya revisado y aprobado:

1. `TASK-20260603-014` routines core
2. `TASK-20260603-015` workouts core
3. `TASK-20260603-016` timers core
4. `TASK-20260603-017` progress core
5. `TASK-20260603-018` plans core
6. `TASK-20260603-019` protocols core

## Contexto obligatorio

Antes de ejecutar, opencode debe leer:

1. `docs/INICIAL.md`
2. `docs/LEY_ARQUITECTURA.md`
3. `docs/AGENTS.md`
4. `docs/CONTRATOS.md`
5. `docs/ENTITY_CATALOG.md`
6. `docs/MODULE_ROADMAP.md`
7. `docs/agents/OPENCODE_EXECUTOR_PROTOCOL.md`
8. todas las tasks core aprobadas

## Objetivo funcional

Conectar los modulos core al flujo real:

```text
Chatwoot -> buffer -> normalizer -> coarseClassifier -> moduleIntentClassifier -> router -> executor -> responseComposer -> Chatwoot
```

## Reglas de integracion

- Priorizar comandos explicitos sobre patrones amplios.
- Mantener `daily-log` como detector amplio solo despues de comandos estructurales.
- No romper `notes`, `tasks`, `session-context`, `reminders`, `daily-log`, `areas` ni `objectives`.
- Cada accion mutante debe exigir `intentConfidence >= 0.75` y `missingData = []`.
- Cada respuesta de accion mutante debe exigir evidencia real (`entityId` + `eventId` si aplica).
- `progress` solo read-only.
- `protocols.evaluate` no ejecuta acciones.
- `timers` solo envia avisos cuando la integracion de dispatcher este explicitamente incluida y testeada.

## Session-context esperado

- `routines.create/activate/pause`: foco `routine`.
- `workouts.start`: foco `workout_session`, activeModule `workouts`, activeFlow `workout_active`.
- `workouts.log-set`: mantener foco en `workout_session`, guardar ejercicio/serie actual.
- `timers.start`: foco opcional en `timer`, contexto con `timerKind`.
- `plans.create/list`: foco `plan` si hay uno.
- `protocols.create/list`: foco `protocol` si hay uno.

## Tests obligatorios

- coarseClassifier detecta cada modulo.
- moduleIntentClassifier detecta cada accion MVP.
- moduleRouter registra cada accion.
- actionExecutor despacha cada handler.
- responseComposer confirma solo con evidencia.
- bufferProcessor ejecuta acciones mutantes sin DeepSeek.
- bufferProcessor usa DeepSeek fallback para desconocidos.
- session-context se actualiza correctamente.
- regresion completa de modulos anteriores.
- Chatwoot scope `7/45/85` intacto.

## Criterio de aceptacion

- `npm run typecheck` pasa.
- `npm test` pasa.
- `npm run build` pasa.
- No hay deploy automatico.
- No hay cambios de secretos.
- No hay cambios de scope Chatwoot.
- No hay objetos DB sin prefijo `sara_`.
- No se confirma accion sin evidencia real.
- Commit usa formato: `feat(TASK-20260603-020): integrar modulos base al pipeline`

## Cierre local

- Integracion realizada por Codex Orquestador.
- Modulos conectados al pipeline:
  - `routines`
  - `workouts`
  - `timers`
  - `progress`
  - `plans`
  - `protocols`
- Cambios realizados:
  - union de modulos en contratos del pipeline;
  - deteccion en `coarseClassifier`;
  - intenciones en `moduleIntentClassifier`;
  - guards en `actionExecutor`;
  - respuestas con evidencia en `responseComposer`;
  - session-context en `bufferProcessor`;
  - stores, modulos y handlers registrados en `server.ts`;
  - tests de integracion/regresion.
- Validaciones:
  - `npm run typecheck`: PASS
  - `npm test`: PASS (790 tests)
  - `npm run build`: PASS
- Sin deploy.
- Sin cambios de secretos.
- Sin cambios de scope Chatwoot.
- Pendiente: deploy controlado, aplicar migraciones nuevas en VPS y validacion productiva Chatwoot.
