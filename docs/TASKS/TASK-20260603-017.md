# TASK-20260603-017
estado: SPEC_PARALLEL_SAFE
owner: opencode
aprobador: Codex Orquestador
objetivo: Implementar core aislado del modulo `progress` MVP como consultas read-only sobre datos existentes, especialmente progreso de gym, sin crear acciones mutantes ni conectar al pipeline Chatwoot.
alcance_permitido: contratos TypeScript de progress; `progressModule`; `progressStore`; parser deterministico MVP; tests unitarios/integracion con fakes; documentacion especifica del modulo; consultas read-only sobre `sara_workout_sessions`, `sara_workout_sets`, `sara_daily_log`, `sara_tasks` y `sara_objectives`
alcance_prohibido: deploy al VPS; cambiar secretos; tocar scope Chatwoot; crear tablas nuevas salvo que Codex lo apruebe explicitamente; tocar tablas sin prefijo `sara_`; tocar objetos de otras apps; modificar `coarseClassifier`, `moduleIntentClassifier`, `moduleRouter`, `actionExecutor`, `responseComposer`, `bufferProcessor` o `server.ts`; escribir DB; emitir eventos; calcular recomendaciones medicas; usar LLM para inventar progreso
validaciones: `npm run typecheck`, `npm test`, `npm run build`
evidencia: salida de validaciones, diff revisable, docs actualizados, commit con formato obligatorio

## Contexto obligatorio

Antes de ejecutar, opencode debe leer:

1. `docs/INICIAL.md`
2. `docs/LEY_ARQUITECTURA.md`
3. `docs/AGENTS.md`
4. `docs/CONTRATOS.md`
5. `docs/ENTITY_CATALOG.md`
6. `docs/MODULE_ROADMAP.md`
7. `docs/agents/OPENCODE_EXECUTOR_PROTOCOL.md`

## Proposito

Responder consultas de progreso con datos ya guardados.

Ejemplo:

```text
como vengo con sentadilla
```

## Arquitectura esperada

No crear tabla en este MVP.

El progreso se deriva de:

- `sara_workout_sessions`
- `sara_workout_sets`
- `sara_daily_log`
- `sara_tasks`
- `sara_objectives`

### Contratos

Crear:

```text
src/contracts/progress.ts
```

Contratos minimos:

- `WorkoutExerciseProgress`
- `ObjectiveProgressSummary`
- `DailyConsistencySummary`
- `GetWorkoutProgressInput`
- `GetWorkoutProgressResult`
- `GetObjectiveProgressInput`
- `GetObjectiveProgressResult`
- `GetDailyConsistencyInput`
- `GetDailyConsistencyResult`
- `ProgressRepository`

### Modulo, store y parser

Crear:

```text
src/modules/progress/progressModule.ts
src/modules/progress/progressParser.ts
src/infra/progressStore.ts
```

Responsabilidades:

- `progressParser`: detecta consultas read-only.
- `progressModule`: valida filtros y arma summaries.
- `progressStore`: solo consultas read-only.

## Parser MVP

Debe soportar:

- `como vengo con sentadilla`
- `progreso sentadilla`
- `progreso objetivo mejorar energia`
- `como voy con mi energia`
- `resumen de progreso`

Reglas:

- Sin dato suficiente, responder con estado vacio, no inventar.
- No escribir DB.
- No emitir eventos.
- No recomendar cambios medicos ni deportivos avanzados.

## Tests obligatorios

- No hay migracion nueva.
- Store solo usa selects/read-only.
- Parser detecta progreso de ejercicio.
- Parser detecta progreso de objetivo.
- Parser detecta resumen general.
- Module retorna empty state sin datos.
- Module calcula ultimo peso/reps por ejercicio.
- Module calcula volumen simple por ejercicio.
- Module resume tareas asociadas a objetivo.
- No se toca pipeline compartido.

## Criterio de aceptacion

- `npm run typecheck` pasa.
- `npm test` pasa.
- `npm run build` pasa.
- No hay deploy.
- No hay cambios de secretos.
- No hay cambios de scope Chatwoot.
- No hay objetos DB nuevos salvo aprobacion explicita.
- No hay escrituras DB.
- No hay cambios en archivos compartidos del pipeline.
- Commit usa formato: `feat(TASK-20260603-017): implementar progress core`

