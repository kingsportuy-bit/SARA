# TASK-20260602-004
estado: DONE
owner: opencode
aprobador: Codex Orquestador
objetivo: Implementar el esqueleto modular de comprension y respuesta de SARA sin crear features de dominio complejas.
alcance_permitido: contratos TypeScript para clasificacion gruesa, clasificacion fina, routing, ejecucion, writer canonico y composicion de respuesta; tests unitarios; documentacion de contratos; mantener bootstrap actual como fallback
alcance_prohibido: implementar Delta, Gym, Finanzas, Protocolos complejos, Planes, Objetivos o Tareas; tocar objetos DB ajenos a `sara_`; desplegar al VPS; cambiar secretos; modificar arquitectura de ingress, buffer o outbound sin instruccion explicita
validaciones: `npm run typecheck`, `npm test`, `npm run build`
evidencia: commits `5321874 feat(TASK-20260602-004): agregar esqueleto modular de comprension` y `e6b0211 fix(TASK-20260602-004): corregir alcance documentado en test suite`; `npm run typecheck` PASS; `npm test` PASS; `npm run build` PASS; revision Codex sin scope creep ni tablas de dominio

## Contexto obligatorio
Antes de ejecutar, opencode debe leer:
1. `docs/INICIAL.md`
2. `docs/LEY_ARQUITECTURA.md`
3. `docs/AGENTS.md`
4. `docs/SECURITY.md`
5. `docs/CONTRATOS.md`
6. `docs/ENTITY_CATALOG.md`
7. `docs/agents/OPENCODE_EXECUTOR_PROTOCOL.md`
8. `docs/INFORME_ANALISIS_ARQUITECTURA_FEATURES.md`

## Contratos TypeScript obligatorios

### `CoarseClassificationInput`

```ts
export interface CoarseClassificationInput {
  schemaVersion: "coarse_classification_input.v1";
  traceId: string;
  messages: Array<{
    id: number;
    content: string;
    createdAt: string;
  }>;
  sessionContext?: {
    activeModule?: string;
    activeFlow?: string;
    focusedEntityType?: string;
    focusedEntityId?: string;
    awaitingConfirmation?: boolean;
  };
}
```

### `CoarseClassificationResult`

```ts
export interface CoarseClassificationResult {
  schemaVersion: "coarse_classification_result.v1";
  traceId: string;
  module: "notes" | "daily-log" | "session-context" | "unknown";
  confidence: number;
  missingData: string[];
  reasoningSummary: string;
}
```

### `ModuleIntentInput`

```ts
export interface ModuleIntentInput {
  schemaVersion: "module_intent_input.v1";
  traceId: string;
  module: CoarseClassificationResult["module"];
  messages: CoarseClassificationInput["messages"];
  sessionContext?: CoarseClassificationInput["sessionContext"];
}
```

### `ModuleIntentResult`

```ts
export interface ModuleIntentResult {
  schemaVersion: "module_intent_result.v1";
  traceId: string;
  module: "notes" | "daily-log" | "session-context" | "unknown";
  action: string;
  confidence: number;
  entities: Record<string, unknown>;
  missingData: string[];
  requiresConfirmation: boolean;
  reasoningSummary: string;
}
```

### `RouteResult`

```ts
export interface RouteResult {
  schemaVersion: "route_result.v1";
  traceId: string;
  module: ModuleIntentResult["module"];
  action: string;
  executable: boolean;
  reason?: string;
}
```

### `ActionExecutionInput`

```ts
export interface ActionExecutionInput {
  schemaVersion: "action_execution_input.v1";
  traceId: string;
  module: ModuleIntentResult["module"];
  action: string;
  entities: Record<string, unknown>;
  requiresConfirmation: boolean;
}
```

### `ActionExecutionResult`

```ts
export interface ActionExecutionResult {
  schemaVersion: "action_execution_result.v1";
  traceId: string;
  status: "executed" | "failed" | "skipped" | "needs_confirmation";
  evidence: Record<string, unknown>;
  stateChanges: Array<{
    entityType: string;
    entityId?: string;
    eventType: string;
    payload: Record<string, unknown>;
  }>;
  error?: string;
}
```

### `ResponseCompositionInput`

```ts
export interface ResponseCompositionInput {
  schemaVersion: "response_composition_input.v1";
  traceId: string;
  messages: CoarseClassificationInput["messages"];
  classification: {
    coarse: CoarseClassificationResult;
    intent: ModuleIntentResult;
  };
  actionResult: ActionExecutionResult;
}
```

### `ResponseCompositionResult`

```ts
export interface ResponseCompositionResult {
  schemaVersion: "response_composition_result.v1";
  traceId: string;
  content: string;
  evidenceUsed: Record<string, unknown>;
}
```

## Reglas obligatorias
- El LLM clasifica y redacta; no decide ejecutar.
- Reglas de negocio se aplican en codigo.
- Si `confidence < 0.75`, no ejecutar accion.
- Si `missingData` no esta vacio, no ejecutar accion.
- Si `requiresConfirmation = true`, devolver `needs_confirmation`.
- No confirmar acciones no ejecutadas.
- No crear tablas de dominio en esta task.
- No implementar `notes.create` todavia; esta task solo deja el esqueleto.
- No adelantar trabajo "ya que estoy".
- No crear `sara_notes`, `sara_tasks`, `sara_plans`, `sara_objectives` ni ninguna tabla/modulo de dominio.

## Archivos esperados
- `src/contracts.ts` actualizado o nuevos contratos en `src/contracts/`
- modulo `coarse-classifier`
- modulo `module-intent-classifier`
- modulo `module-router`
- modulo `action-executor`
- modulo `response-composer`
- tests unitarios para cada contrato o modulo nuevo
- `docs/CONTRATOS.md` actualizado
- `docs/TEST_SUITE.md` actualizado
- `docs/CHANGELOG.md` actualizado

## Criterio de aceptacion
- Compila TypeScript.
- Pasan todos los tests.
- El bootstrap actual sigue funcionando como fallback.
- No se ejecutan acciones reales.
- No se crean tablas nuevas salvo que el orquestador lo autorice explicitamente en una task posterior.
- El diff no contiene decisiones de producto no solicitadas.
- El diff no contiene trabajo adelantado fuera del alcance, aunque sea funcional.
- El commit usa formato: `feat(TASK-20260602-004): agregar esqueleto modular de comprension`

## Si falta informacion
opencode debe detenerse y registrar:

```text
BLOQUEO
Task: TASK-20260602-004
Falta:
Impacto:
Decision requerida:
Opciones seguras:
```
