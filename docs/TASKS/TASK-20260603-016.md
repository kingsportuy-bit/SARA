# TASK-20260603-016
estado: SPEC_PARALLEL_SAFE
owner: opencode
aprobador: Codex Orquestador
objetivo: Implementar core aislado del modulo `timers` MVP para descansos y temporizadores interactivos, sin conectarlo todavia al dispatcher ni al pipeline Chatwoot.
alcance_permitido: migracion `sara_timers`; contratos TypeScript de timers; `timersModule`; `timersStore`; parser deterministico MVP; tests unitarios/integracion con fakes; documentacion especifica del modulo
alcance_prohibido: deploy al VPS; cambiar secretos; tocar scope Chatwoot; tocar tablas sin prefijo `sara_`; tocar objetos de otras apps; modificar `coarseClassifier`, `moduleIntentClassifier`, `moduleRouter`, `actionExecutor`, `responseComposer`, `bufferProcessor`, dispatcher de reminders o `server.ts`; enviar mensajes Chatwoot; crear sesiones de gym; crear recordatorios persistentes de calendario; usar LLM para inventar tiempos; confirmar acciones sin evidencia real
validaciones: `npm run typecheck`, `npm test`, `npm run build`
evidencia: salida de validaciones, diff revisable, docs actualizados, commit con formato obligatorio

## Contexto obligatorio

Antes de ejecutar, opencode debe leer:

1. `docs/INICIAL.md`
2. `docs/LEY_ARQUITECTURA.md`
3. `docs/AGENTS.md`
4. `docs/CONTRATOS.md`
5. `docs/ENTITY_CATALOG.md`
6. `docs/MODULE_ROADMAP.md`
7. `docs/agents/OPENCODE_EXECUTOR_PROTOCOL.md`

## Proposito

Crear temporizadores cortos e interactivos, especialmente descansos de gym.

Ejemplo:

```text
descanso 90 segundos
```

## Arquitectura esperada

### DB

Crear:

- `sara_timers`

Campos minimos:

- `id uuid primary key`
- `schema_version text not null`
- `kind text not null`
- `status text not null`
- `title text not null`
- `duration_seconds integer not null`
- `due_at timestamptz not null`
- `related_entity_type text`
- `related_entity_id uuid`
- `account_id bigint not null`
- `inbox_id bigint not null`
- `conversation_id bigint not null`
- `trace_id uuid`
- `created_at timestamptz not null`
- `updated_at timestamptz not null`
- `fired_at timestamptz`
- `canceled_at timestamptz`

Kinds:

- `workout_rest`
- `generic`

Estados:

- `pending`
- `fired`
- `canceled`

Eventos:

- `timer_started`
- `timer_fired`
- `timer_canceled`

RPCs minimas:

- `sara_start_timer`
- `sara_cancel_timer`
- `sara_claim_due_timers`
- `sara_mark_timer_fired`

### Contratos

Crear:

```text
src/contracts/timers.ts
```

Contratos minimos:

- `TimerRecord`
- `StartTimerInput`
- `StartTimerResult`
- `CancelTimerInput`
- `CancelTimerResult`
- `ClaimDueTimersInput`
- `ClaimDueTimersResult`
- `MarkTimerFiredInput`
- `MarkTimerFiredResult`
- `TimersRepository`

### Modulo, store y parser

Crear:

```text
src/modules/timers/timersModule.ts
src/modules/timers/timersParser.ts
src/infra/timersStore.ts
```

Responsabilidades:

- `timersParser`: parsea duraciones simples.
- `timersModule`: valida duracion y scope.
- `timersStore`: accede a Supabase/RPC.

## Parser MVP

Debe soportar:

- `descanso 90 segundos`
- `timer 2 minutos`
- `cancelar timer`

Reglas:

- Duracion debe ser positiva.
- Maximo MVP: 30 minutos.
- No enviar mensajes en esta task.
- No conectar con workouts todavia.

## Tests obligatorios

- migracion solo crea objetos `sara_`.
- RLS habilitado y anon/authenticated revocado.
- status/kind validos.
- duration positiva.
- RPC start crea timer y emite evento.
- RPC cancel cancela y emite evento.
- RPC claim_due_timers reclama vencidos por scope.
- Parser duraciones segundos/minutos.
- Module valida duracion maxima.
- Store llama RPCs correctas.
- No se toca pipeline compartido.

## Criterio de aceptacion

- `npm run typecheck` pasa.
- `npm test` pasa.
- `npm run build` pasa.
- No hay deploy.
- No hay cambios de secretos.
- No hay cambios de scope Chatwoot.
- No hay objetos DB sin prefijo `sara_`.
- No hay cambios en archivos compartidos del pipeline.
- Commit usa formato: `feat(TASK-20260603-016): implementar timers core`

## Cierre local

- Core aislado implementado.
- Hardening Codex aplicado sobre migracion:
  - firma `sara_start_timer` sin parametros opcionales antes de obligatorios;
  - indice renombrado a prefijo `sara_`;
  - comentarios ajustados para mantener verde el guard de migraciones.
- Sin deploy.
- Sin cambios de secretos.
- Sin cambios de scope Chatwoot.
- Sin cambios en archivos compartidos del pipeline.
- Validaciones globales posteriores:
  - `npm run typecheck`: PASS
  - `npm test`: PASS (769 tests)
  - `npm run build`: PASS
